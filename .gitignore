node_modules/
dist/
dist-electron/
release/
coverage/
*.log
.DS_Store
.vite/

# Never let the app's local runtime state leak into the repo.
GameOfLife/

# Stray artifact written into cwd by ruflo/ruvector tooling, not part of the app
ruvector.db
